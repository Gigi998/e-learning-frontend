export enum Endpoints {
  LOGIN = '/login',
  LOGOUT = '/logout,',
  REGISTER = '/register',
  REFRESH = '/refresh',
}

export enum Params {
  EMPTHY = '',
  FREEBOOKS = '?freeBooks=true',
}

export enum Roles {
  ADMIN = 'ADMIN',
  TEACHER = 'TEACHER',
  STUDENT = 'STUDENT',
}
