export enum Endpoints {
  LOGIN = '/login',
  LOGOUT = '/logout,',
  REGISTER = '/register',
  REFRESH = '/refresh',
}

export enum Roles {
  ADMIN = 'ADMIN',
  TEACHER = 'TEACHER',
  STUDENT = 'STUDENT',
}

export const DEFAULT_STUDENTS_TAKE = 4;
export const DEFAULT_BOOKS_TAKE = '6';
